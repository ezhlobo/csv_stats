extends ../layout

block content
  .page-header
    h1 File name: «#{file.originalname}»

  if !file.parsed_at
    .lead#time_area(data-created-at=file.created_at)
      strong
        span [
        span 00:00:000
        span ]

      span  Loading...

    #parsed_area

    script.
      (function() {
        var $area = $('#time_area');
        var createdAt = parseInt($area.data('created-at'));

        var updateTime = function() {
          var diff = moment(moment().diff(createdAt)).format('mm:ss:SSS')

          $area.html('<strong>[' + diff + ']</strong> <span>Loading...</span>');
        };

        var timer = setInterval(updateTime, 5);

        socket = io.connect('http://localhost:3535/#{file.name}');
        socket.on('parsed', function (data) {
          clearInterval(timer);
          $area.find('> span').html(' Loaded')
          $('#parsed_area').html(data.table);
        });
      })();

  else
    .lead
      span Loaded

    #parsed_area
      != table
