nav.navbar.navbar-default.navbar-fixed-top
  .container
    .navbar-header
      button.navbar-toggle.collapsed(type="button" data-toggle="collapse" data-target="#navbar")
        span.sr-only Toggle navigation

        .icon-bar

        .icon-bar

        .icon-bar

    .collapse.navbar-collapse#navbar
      .nav.navbar-nav
        - links = []
        - links.push({title: "Home", path: "/"})
        - links.push({title: "Files", path: "/files"})
        - links.push({title: "Statistics", path: "/statistics"})

        each link in links
          li(class=current_path == link.path && "active")
            a(href=link.path remote)
              = link.title
